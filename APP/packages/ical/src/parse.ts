// Parse an .ics string into normalized events.
// Real implementation lands in Phase 2.

import type { NormalizedEvent } from './index.js';

export function parseICal(_icsText: string): NormalizedEvent[] {
  return [];
}
